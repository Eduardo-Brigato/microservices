server:
  port: 8888
spring:
  application:
    name: erudio-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Eduardo-Brigato/config-server
          default-label: main
          search-paths:
            - 'greeting-service*'
